{
  "name": "proveneio",
  "version": "0.9.1",
  "description": "Portable, signed evidence receipts for AI-generated code changes",
  "license": "Apache-2.0",
  "type": "module",
  "bin": {
    "provene": "dist/cli.js"
  },
  "engines": {
    "node": ">=22.6"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc -p tsconfig.json",
    "typecheck": "tsc -p tsconfig.json --noEmit",
    "test": "node --test test/*.test.ts",
    "conformance": "node --test test/conformance.test.ts",
    "prepublishOnly": "npm run build",
    "test:coverage": "node --test --experimental-test-coverage --test-reporter=lcov --test-reporter-destination=coverage/lcov.info --test-reporter=spec --test-reporter-destination=stdout 'test/*.test.ts'",
    "samples": "node scripts/emit-samples.ts ../../.samples",
    "site:sync": "node -e \"const{copyFileSync}=require('node:fs');for(const[a,b]of[['../../spec/schema/provene-receipt-v0.1.schema.json','../../docs/schema/receipt/v0.1.json'],['../../spec/schema/provene-aggregate-v0.1.schema.json','../../docs/schema/aggregate/v0.1.json'],['../../spec/schema/provene-policy-v1.schema.json','../../docs/schema/policy/v1.json']])copyFileSync(a,b)\""
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  },
  "keywords": [
    "provenance",
    "attestation",
    "ai",
    "agent",
    "code-review",
    "supply-chain",
    "in-toto",
    "sigstore",
    "receipts"
  ],
  "repository": {
    "type": "git",
    "url": "git+https://github.com/provene-hq/provene.git"
  },
  "homepage": "https://github.com/provene-hq/provene#readme",
  "bugs": {
    "url": "https://github.com/provene-hq/provene/issues"
  }
}

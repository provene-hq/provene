{
  "name": "provene-monorepo",
  "private": true,
  "scripts": {
    "install:cli": "npm --prefix packages/cli install",
    "build": "npm --prefix packages/cli run build",
    "typecheck": "npm --prefix packages/cli run typecheck",
    "pretest": "node -e \"const{globSync}=require('node:fs');const n=globSync('packages/cli/test/*.test.ts').length;if(n<8){console.error('pretest: only '+n+' test file(s) discovered -- the glob is not matching, so `npm test` would exit 0 having run nothing');process.exit(1)}\"",
    "test": "node --test \"packages/cli/test/*.test.ts\"",
    "conformance": "python3 spec/conformance/runner/run.py || python spec/conformance/runner/run.py",
    "schema": "npm --prefix packages/cli run samples && python spec/conformance/runner/validate_receipts.py .samples spec/examples",
    "lint:action": "python3 spec/conformance/runner/lint_action.py action.yml || python spec/conformance/runner/lint_action.py action.yml",
    "self-test": "python3 spec/conformance/runner/self_test.py || python spec/conformance/runner/self_test.py",
    "metrics": "node planning/metrics/collect.mjs",
    "metrics:report": "node planning/metrics/collect.mjs report"
  }
}
